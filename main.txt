#include <iostream>
using namespace std;

int getMax(int value);

int main() {
  int avg(0);
  int max(0);

  max = getMax(avg);
  return 0;
}

int getMax(int value) {
  return value*2;
}