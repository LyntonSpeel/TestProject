#include <iostream>
#include "getMax.txt"
#include "getMin.txt"
using namespace std;

int main() {
  int avg(0);
  int avgMax(0), avgMin(0);

  avgMax = getMax(avg);
  avgMin = getMin(avg);
  return 0;
}